<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects - Builder6</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white font-sans antialiased selection:bg-blue-500 selection:text-white min-h-screen flex flex-col">

    {% render 'partials/header.liquid', is_editor: false, user: user %}

    <main class="flex-1 container mx-auto px-6 py-8 max-w-5xl">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-3xl font-bold text-white tracking-tight">Projects</h2>
            <a href="/app/new" class="bg-[#007AFF] hover:bg-[#0062cc] text-white px-4 py-2 rounded-xl text-sm font-medium transition-all shadow-lg shadow-blue-900/20 hover:shadow-blue-900/40 flex items-center gap-2 transform active:scale-95">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" /></svg>
                New Project
            </a>
        </div>

        {% if projects.size == 0 %}
        <div class="text-center py-20 bg-[#1c1c1e] rounded-3xl border border-white/5">
            <div class="w-16 h-16 bg-white/5 rounded-full flex items-center justify-center mx-auto mb-4 text-3xl">
                🚀
            </div>
            <h3 class="text-xl font-medium text-white mb-2">No projects yet</h3>
            <p class="text-gray-400 mb-6">Create your first project to organize your pages.</p>
            <a href="/app/new" class="text-blue-400 hover:text-blue-300 hover:underline">Create Project &rarr;</a>
        </div>
        {% else %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for project in projects %}
            <a href="/app/{{ project.slug | default: project._id }}" class="group block bg-[#1c1c1e] border border-white/10 rounded-2xl overflow-hidden hover:border-blue-500/50 transition-all hover:shadow-2xl hover:shadow-blue-900/10 hover:-translate-y-1">
                <div class="p-5">
                    <div class="flex justify-between items-start mb-3">
                        <h3 class="text-white font-semibold text-lg truncate pr-2 group-hover:text-blue-400 transition-colors">{{ project.name }}</h3>
                        <span class="text-xs text-gray-500 bg-white/5 px-2 py-1 rounded-full border border-white/5">{{ project.modified | date: "%Y-%m-%d" }}</span>
                    </div>
                    <p class="text-gray-400 text-sm line-clamp-2 mb-4 h-10">{{ project.description | default: "No description" }}</p>
                    <div class="flex justify-between items-center mt-2">
                        <span class="text-xs text-gray-500">Project</span>
                        <span class="text-blue-400 text-xs font-medium opacity-0 group-hover:opacity-100 transition-opacity flex items-center gap-1">
                            Open Project <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" /></svg>
                        </span>
                    </div>
                </div>
            </a>
            {% endfor %}
        </div>
        {% endif %}
    </main>


    <!-- Modal Removed -->

    <script>
        // No createProject script needed here anymore
    </script>

</body>
</html>
